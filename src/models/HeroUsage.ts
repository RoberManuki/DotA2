class HeroUsage {
  hero_id: number;

  times_used: number;

  constructor(hero_id: number) {
    this.hero_id = hero_id;
    this.times_used = 1;
  }
}
//= ===========================================================================>

export default HeroUsage;
